0. Напоминание:
   1) Создание массива из списка
   2) zeros, ones, zeros_like, ones_like, eye
   3) подпакет np.random

1. Дополнительная информация по созданию массивов:
   1) np.arange - не ГЕНЕРАТОР!!!
   2) np.linspace

2. Напоминание про индексацию:
   1) одинарный и двойной индексы
   2) одинарные, двойные срезы
   3) булевые маски

3. Дополнительная информация про булевые маски:
   1) np.where - для поиска индексов, где выполняется условие

4. Напоминание про форму:
   1) параметр shape
   2) метод reshape
   3) метод ravel для приведения в одномерный вид

5. Напоминание про математику массивов
   1) перегрузка операторов +,-,*,**,/,//,==,<,>
   2) оператор @ или функция np.dot

6. Дополнительные математические операции
   1) np.sqrt - извлечение корня
   2) np.log2, np.log
   3) np.exp2, np.exp
   4) np.pi, np.cos, np.sin, np.tan

7. Бродкастинг
   1) общая информация
   2) пример со скаляром
   3) пример с прибавлением вектора к каждой строке матирцы
         1. создание соразмерной матрицы
         2. np.tile(v, (4,1))
         3. x + v
   4) правила бродкастинга
   5) другие примеры

8. Практический пример

9. Подпакет np.linalg

10. Полиномы
